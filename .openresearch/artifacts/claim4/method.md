# Claim 4 method

The exact factor exp(m/log(1/rho)) is evaluated on 13 predeclared rho values,
with independently computed tau_0.05. A constant-factor mutation must fail.
The source wording is audited separately from the factor calculation.
