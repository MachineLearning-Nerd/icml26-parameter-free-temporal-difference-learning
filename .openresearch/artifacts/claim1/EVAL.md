# Claim 1 evaluation

The run verifier exits nonzero unless assumptions, all explicit-bound checks,
the scalar independent checker, the algebraic replay, and the constant-step
negative control pass. The verdict remains tied to the exact schedule; results
from the historical reciprocal step size are rejected.
