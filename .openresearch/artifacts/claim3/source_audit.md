# Claim 3 source audit

Theorem 4.12 (`thm:reg_markov`, ar5iv `S4.Thmtheorem12`) uses
`lambda=1/sqrt(T)` and `eta0=lambda/[C ln^2(T)+C'+8+2lambda^2]`.
Neither omega, a projection radius, nor an averaging window reaches the update
rule. The theorem returns the last regularized TD iterate.
