# Claim 5 evaluation

Status before baseline run: `BLOCKED` pending HF `cpu-upgrade` execution.

Required evidence: exact raw output, independent checker output, negative-control output, Git SHA, locked environment hash, actual CPU allocation, and runtime. These are emitted by the fixed command and will be committed additively on a child after the baseline node freezes.

