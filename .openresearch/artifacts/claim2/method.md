# Claim 2 method

The exact Theorem 4.10 formula is evaluated at three omega values. Ratios must
match omega ratios, while a formula with omega deleted must be rejected. A
bracketed binary search in log(T), not a formula-selected trial horizon, finds
the first point satisfying every stated horizon condition.
